import React, { Component } from "react";
import { Dialog } from "@material-ui/core";

class StandardWidgetDialog extends Component {
  render() {
    const { open, onClose,onCreateWidget } = this.props;
    return (
      <Dialog>

      </Dialog>
    );
  }
}

export default StandardWidgetDialog;