/*
import React, {Component} from "react";
import {Button, Dialog, DialogActions, DialogContent, DialogTitle} from "@material-ui/core";
import PropTypes from "prop-types";

class NotesheetTimelineView extends Component {
    render() {
        const {open, onClose, ...rest} = this.props;
        return (
            <Dialog fullWidth={true} maxWidth={"md"} open={open} onClose={onClose} {...rest}>
                <DialogTitle title={"Notesheet Movement"}/>
                <DialogContent>

                </DialogContent>
                <DialogActions>
                    <Button variant={"contained"} color={"secondary"} onClick={onClose}>Close</Button>
                </DialogActions>
            </Dialog>
        );
    }
}

NotesheetTimelineView.propTypes = {
    open: PropTypes.bool.isRequired,
    onClose: PropTypes.func.isRequired
};
export default NotesheetTimelineView;*/
